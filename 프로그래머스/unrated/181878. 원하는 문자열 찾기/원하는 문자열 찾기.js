function solution(myString, pat) {
    const lowerCaseStr = myString.toLowerCase();
    const lowerPat = pat.toLowerCase();
    console.log(lowerCaseStr.split(" "))
    if(lowerCaseStr.includes(lowerPat)){
        
    return 1
    }else return 0
}